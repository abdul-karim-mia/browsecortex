# NPM Configuration for BrowseCortex

# Only publish from CI/CD
# For local publishing, use: npm publish packages/relay --access public

# Strict mode
strict-peer-dependencies=false

# Metadata
@browsecortex:registry=https://registry.npmjs.org/
