{
  "name": "browsecortex-relay",
  "version": "1.2.0",
  "description": "WebSocket↔HTTP/SSE relay so external MCP agents can drive BrowseCortex.",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "browsecortex-relay": "dist/index.js",
    "browsecortex-mcp": "dist/stdio.js"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "node --watch --import tsx src/index.ts --token ${RELAY_DEV_TOKEN:-dev-token-12345}",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "@types/ws": "^8.5.12",
    "tsx": "^4.19.0",
    "typescript": "^5.6.2",
    "vitest": "^2.1.0"
  }
}
