{
  "name": "browsecortex-relay",
  "version": "1.0.0",
  "description": "WebSocket↔HTTP/SSE relay so external MCP agents can drive BrowseCortex.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "browsecortex-relay": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "node --watch --loader tsx src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "@types/ws": "^8.5.12",
    "typescript": "^5.6.2"
  }
}
