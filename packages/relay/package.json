{
  "name": "browsecortex-relay",
  "version": "1.0.4",
  "description": "WebSocket↔HTTP/SSE relay so external MCP agents can drive BrowseCortex.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "browsecortex-relay": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "node --watch --import tsx src/index.ts --token dev-token-12345",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "@types/ws": "^8.5.12",
    "tsx": "^4.19.0",
    "typescript": "^5.6.2"
  }
}
